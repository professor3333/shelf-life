# REHEARSAL BRANCH — not main. This names the synthetic prerelease so that a
# throwaway Render service built from this branch executes the one path the
# public service never has: fetch a release during Render's own build, serve
# it, and pass await_release + smoke (with ALLOW_SYNTHETIC=1) at a public URL.
# The model is fitted on the synthetic panel and means nothing; the service is
# deleted after the rehearsal. main's MODEL_TAG stays empty until the real H=7
# release exists.
artifact-rehearsal-2026-09-11
