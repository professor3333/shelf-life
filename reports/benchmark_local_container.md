# Service benchmark — behaviour under load

| | |
|---|---|
| Generated by | `python -m benchmarks.service` |
| Data | target `http://localhost:8775` (`local_container`) · model `05-xgboost_engineered` on the `synthetic` panel · release `artifact-rehearsal-2026-09-11` |
| Code | measured on 2026-09-12; absolute figures are this target's, not the free instance's unless the label says so |

_Regenerate rather than edit._

Each section is a behaviour with a pass condition; the numbers beside it say what
passing meant here. Absolute latencies on a laptop core are roughly ten times
faster than the free instance's tenth of a CPU — read the shape, not the number.

| section | pass | detail |
|---|---|---|
| warm /predict, sequential | yes | 50 requests, all 200 |
| concurrent /predict ×8 | yes | 100 requests, 100 × 200, 0 × 5xx |
| concurrent /rank ×4, 250 postings each | yes | 8 requests, 8 × 200; every ranking flagged exactly 20 |
| peak RSS across 5 rankings of 250 | yes | 256 MB → 256 MB (+0 MB); a leak would climb every round |
| malformed and oversized requests | yes | one over the batch cap: 422 in 7 ms; a 5 MB title: 422 in 71 ms; a 2,000-posting body: 422 in 28 ms; nonsense fields: 422 in 2 ms; wrong types: 422 in 2 ms |
| a burst of 32 with no rate limit | yes | all 32 answered in 1.11 s wall; none refused, none 5xx — there is no limit to hit, so a burst costs everyone latency rather than anyone an error |

## Numbers

| section | batch | burst | concurrency | growth_mb | max_ms | n | p50_ms | p95_ms | rss_after_mb | rss_before_mb | wall_s |
|---|---|---|---|---|---|---|---|---|---|---|---|
| warm /predict, sequential | — | — | — | — | 259.4 | 50.0 | 28.7 | 103.0 | — | — | — |
| concurrent /predict ×8 | — | — | 8.0 | — | 402.2 | 100.0 | 270.0 | 354.1 | — | — | — |
| concurrent /rank ×4, 250 postings each | 250.0 | — | 4.0 | — | 8538.6 | 8.0 | 8432.5 | 8551.4 | — | — | — |
| peak RSS across 5 rankings of 250 | — | — | — | 0.0 | — | — | — | — | 256.3 | 256.3 | — |
| malformed and oversized requests | — | — | — | — | — | — | — | — | — | — | — |
| a burst of 32 with no rate limit | — | 32.0 | — | — | 1091.4 | 32.0 | 1016.7 | 1091.4 | — | — | 1.1 |

## What is deliberately not here

A rate limit. The service has none (`docs/design.md` §7b; the non-goals), so the
last section measures what that costs — a burst queues at the single worker and
every request is answered, slowly — rather than testing a limit that does not
exist. A public free instance with no limit can be made slow by one caller; it
cannot be made to answer wrongly, which is what the sections above check.
