# Service benchmark — behaviour under load

| | |
|---|---|
| Generated by | `python -m benchmarks.service` |
| Data | target `http://localhost:8776` (`local_container`) · model `05-xgboost_engineered` on the `synthetic` panel · release `artifact-rehearsal-2026-09-11` |
| Code | measured on 2026-09-12; absolute figures are this target's, not the free instance's unless the label says so |

_Regenerate rather than edit._

Each section is a behaviour with a pass condition; the numbers beside it say what
passing meant here. Absolute latencies on a laptop core are roughly ten times
faster than the free instance's tenth of a CPU — read the shape, not the number.

| section | pass | detail |
|---|---|---|
| warm /predict, sequential | yes | 50 requests, all 200 |
| concurrent /predict ×8 | yes | 100 requests, 100 × 200, 0 × 5xx |
| concurrent /rank ×4, 250 postings each | yes | 8 requests: 7 × 200, 1 × 429 (the per-address budget); every served ranking flagged exactly 20 |
| peak RSS across 5 rankings of 250 | yes | 294 MB → 294 MB (+0 MB); a leak would climb every round |
| malformed and oversized requests | yes | one over the batch cap: 422 in 9 ms; a 5 MB title: 413 in 34 ms; a 2,000-posting body: 429 in 7 ms; nonsense fields: 422 in 3 ms; wrong types: 422 in 3 ms |
| a burst of 32 on /predict, which has no budget | yes | all 32 answered in 1.79 s wall; none refused, none 5xx — a single posting is cheap enough that a burst costs latency rather than an error |
| 10 expensive requests at once against a budget of 6 | yes | 0 × 200, 10 × 429; no 5xx |

## Numbers

| section | batch | burst | concurrency | growth_mb | max_ms | n | p50_ms | p95_ms | refused_429 | rss_after_mb | rss_before_mb | wall_s |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| warm /predict, sequential | — | — | — | — | 26.4 | 50.0 | 24.2 | 26.1 | — | — | — | — |
| concurrent /predict ×8 | — | — | 8.0 | — | 555.1 | 100.0 | 271.5 | 497.5 | — | — | — | — |
| concurrent /rank ×4, 250 postings each | 250.0 | — | 4.0 | — | 7815.5 | 7.0 | 7725.8 | 7854.4 | 1.0 | — | — | — |
| peak RSS across 5 rankings of 250 | — | — | — | 0.0 | — | — | — | — | — | 293.9 | 293.9 | — |
| malformed and oversized requests | — | — | — | — | — | — | — | — | — | — | — | — |
| a burst of 32 on /predict, which has no budget | — | 32.0 | — | — | 1776.8 | 32.0 | 1449.6 | 1759.8 | — | — | — | 1.8 |
| 10 expensive requests at once against a budget of 6 | — | — | — | — | — | — | — | — | 10.0 | — | — | — |

## What is deliberately not here

Authentication, and a limit on the cheap routes. `/predict` and `/health` have
no budget, so a burst there queues at the single worker and every request is
answered, slowly. The expensive routes carry a per-address budget
(`api/protection.py`): a courtesy limit on one free instance, not a security
boundary — the last section shows it refusing with 429 rather than slowing
everyone. A public free instance can still be made slow by one caller on the
cheap routes; it cannot be made to answer wrongly, which is what is checked.
